Requirement: Install docker

Install steps:
cd to working folder:
0. run 'make help' for more information 
1. run: 'make build_container'
2. run: 'make macrun' 
3. -> inside container's terminal: run './init_db.sh' and follow initial set up instructions ('chmod +x init.sh' if not executable)
